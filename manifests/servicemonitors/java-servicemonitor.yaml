apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: java-service-metrics
  labels:
    team: backend
spec:
  selector:
    matchLabels:
      app: my-java-app
  endpoints:
  - port: metrics
    path: /actuator/prometheus
    interval: 15s
