apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: node-service-metrics
spec:
  selector:
    matchLabels:
      app: my-node-app
  endpoints:
  - port: metrics
    path: /metrics
    interval: 15s
